#!/bin/sh
cd "${0%/*}" || exit 1

rm -rf constant/polyMesh
echo "Mesh is removed!"

rm -f LOG.*
echo "Log files are removed!"

rm -rf [1-9]*
echo "Saved files are removed!"

rm -rf postProcessing
echo "Post processing is removed!"
